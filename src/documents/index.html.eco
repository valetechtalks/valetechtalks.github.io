---
layout: default
---

<% for section in @sections : %>
  <!-- <%= section.toUpperCase() %> -->

  <section class="<%= section %>" id="<%= section %>">
    <%- @partial("section/#{section}", @) %>
  </section>

  <!-- / <%= section.toUpperCase() %> -->
<% end %>
